{
  "name": "fleetform",
  "version": "1.2.9",
  "description": "A docker container infrastructure as code cli tool.",
  "main": "dist/index.js",
  "author": "HalsMaulMajo",
  "license": "MIT",
  "private": false,
  "tags": [
    "docker",
    "container",
    "iac",
    "infrastructure",
    "fleetform",
    "typescript",
    "cli"
  ],
  "bin": {
    "ff": "./bin/ff",
    "ffdev": "./bin/ffdev"
  },
  "scripts": {
    "dev": "nodemon --ext '*.ts' -w ${PWD}/src -x 'npm run build; npm run start'",
    "start-watch": "nodemon -w ./resources -x 'npm run start'",
    "build-watch": "nodemon --ext '*.ts' -w ${PWD}/src -x 'npm run build'",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "tsnode": "ts-node src/index.ts",
    "test": "echo 'No test exist!'",
    "release": "npm up; npm run build; npm run test; npm publish"
  },
  "devDependencies": {
    "@types/cors": "^2.8.12",
    "@types/dockerode": "^3.3.1",
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.26",
    "nodemon": "^2.0.13",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "cmdy": "^1.0.0",
    "cors": "^2.8.5",
    "docker-modem": "^3.0.3",
    "dockerode": "^3.3.1",
    "express": "^4.17.3"
  }
}
