{
  "name": "fleetform",
  "version": "1.2.1",
  "description": "A docker manager.",
  "main": "dist/index.js",
  "author": "HalsMaulMajo",
  "license": "MIT",
  "private": false,
  "tags": [
    "docker",
    "container",
    "iac",
    "incrastructure",
    "fleetform",
    "typescript",
    "cli"
  ],
  "bin": {
    "fleetform": "node ./dist/index.js",
    "ff": "node ./dist/index.js",
    "ffdev": "./node_modules/.bin/ts-node ./src/index.ts"
  },
  "scripts": {
    "dev": "nodemon --ext '*.ts' -w ${PWD}/src -x 'npm run build; npm run start'",
    "start-watch": "nodemon -w ./resources -x 'npm run start'",
    "build-watch": "nodemon --ext '*.ts' -w ${PWD}/src -x 'npm run build'",
    "start": "node dist/index.js",
    "build": "tsc -p tsconfig.json",
    "tsnode": "ts-node src/index.ts",
    "test": "echo 'No test exist!'",
    "release": "npm up -g npm@latest; npm up; npm run compile; npm run test; npm publish"
  },
  "devDependencies": {
    "@types/dockerode": "^3.3.1",
    "@types/node": "^16.11.21",
    "nodemon": "^2.0.13",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.5"
  },
  "dependencies": {
    "cmdy": "^1.0.0",
    "docker-modem": "^3.0.3",
    "dockerode": "^3.3.1"
  }
}